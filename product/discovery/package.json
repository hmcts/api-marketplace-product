{
  "name": "discovery",
  "version": "1.0.0",
  "description": "Discovery report generator — converts the API Marketplace discovery markdown into a self-contained HTML view.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "marked": "^18.0.4"
  }
}
